#include <bits/stdc++.h>
using namespace std;

int main() {
    string linha;
    cout << "Digite a última linha:\n";
    getline(cin, linha);

    cout << "Número de caracteres na última linha: " << linha.size() << endl;
    return 0;
}
